**改动内容**：（一个 PR 一件事）

**属于哪个 Gate / Issue**：()

**协议是否变更**：否 / 是（若是有，Schema / OpenAPI / 实现 / 测试 / 文档五处已同步）

**验证**：

- [ ] `go vet ./...` / `go test ./...` 全绿
- [ ] `bash scripts/check-governance.sh` 通过（涉及文档时）
- [ ] 未引入已移除的商业能力（Trial / License / Cloud 等）
- [ ] 状态描述诚实（mock 通过 ≠ 硬件验证）
