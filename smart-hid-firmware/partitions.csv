# Smart HID Partition Table
# Name,   Type, SubType,  Offset,  Size, Flags
# 预留双 OTA 分区（V1 不启用 OTA 更新，但 partition table 先就位）。
# M1-G3 起 app 扩到 1536K：NimBLE + esp_http_client + provisioning 组件
# 使固件超过 1M（当时 1.18M），且需为后续 OTA 预留余量（8MB flash 充足）。
# NVS 0x4000（identity/runtime_config(rt_active/rt_pending)/nimble bond 等）。
nvs,      data, nvs,     0x9000,  0x8000
otadata,  data, ota,     0x11000, 0x2000
phy_init, data, phy,     0x13000, 0x1000
factory,  app,  factory, 0x20000, 1536K
ota_0,    app,  ota_0,   ,        1536K
ota_1,    app,  ota_1,   ,        1536K
